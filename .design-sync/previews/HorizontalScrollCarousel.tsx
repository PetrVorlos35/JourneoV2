import { HorizontalScrollCarousel } from 'journeov2';

// A scroll-driven horizontal carousel (desktop only, md+). At scroll position 0
// it shows the first panel — a hero headline beside a glass countdown card.
// Single cell: it's framer-scroll-linked and self-contained (no props).

export const Default = () => <HorizontalScrollCarousel />;
